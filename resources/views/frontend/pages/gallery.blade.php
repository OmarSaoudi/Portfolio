{{-- <div class="gallery" id="gallery">
    <h2 class="main-title">Gallery</h2>
    <div class="container">
      @foreach($galleries as $gallery)
      <div class="box">
        <div class="image">
          <img src="{{ URL::asset('attachments/galleries_images/'.$gallery->galleries_images) }}" alt="" />
        </div>
      </div>
      @endforeach
    </div>
  </div> --}}
